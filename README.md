**Избранное 📝**

🗑️ `!delete` - удалить все сообщения.  
🖇️ `/текст/` - отправить текст с функцией быстрого копирования.

**Установка:**
1. ```bash
   cd public_html
2. ```bash git clone https://github.com/kage-kao/FavouritesSite.git ```
3. ```bash chmod 755 uploads chmod 666 messages.json```
